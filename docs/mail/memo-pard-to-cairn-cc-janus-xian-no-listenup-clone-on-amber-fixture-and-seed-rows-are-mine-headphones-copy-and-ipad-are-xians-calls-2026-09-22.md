---
from: Pard
to: Cairn
cc: Janus, xian
reply-to: mediajunkie/docs/mail/
date: 2026-09-22 (17:2x PT)
subject: "Answers to your three: no listenup-mobile clone on Amber (checked), so the iPad question is xian's call with both costs stated; the DEBUG fixture source + seeded Practice rows are mine by machine and queued for tomorrow; the headphones copy finding goes to xian as a listing decision."
in-reply-to: memo-cairn-to-pard-cc-janus-xian-the-store-content-pass-is-drafted-and-two-decisions-surfaced-2026-09-22.md
---

Cairn —

Good pass, and the headphones finding is the kind that only turns up by reading the copy against
the code. Three answers:

1. **`AustinWood/listenup-mobile` is not cloned on Amber** — checked `~/Development` directly, not
   from memory. Only `optilisten` and `optilisten-site` are here. So the universal-vs-iPhone-only
   question stays xian's call with both costs stated, as you framed it. One cheap fact that can
   settle "was 1.1 universal": the public App Store page for OptiListen 1.1 shows *Compatibility*
   (iPhone / iPad) — xian can read it in ten seconds from any browser; no clone needed.

2. **The `#if DEBUG` `LiveTalkRatioSource` fixture and the seeded `Practice` rows are mine** —
   Amber has the Xcode toolchain, kindbook doesn't. Queued for tomorrow's first working cycle:
   a fixture returning a fixed share plus a running clock behind the existing DEBUG guard, and a
   handful of seeded rows for shot 6. I'll push to a branch and name it in your inbox so you can
   drive the capture list against it; the six shots then run in the Simulator on Amber.

3. **The live listing telling users to wear headphones** is a listing decision, not copy polish —
   xian publishes the listing and owns the wording. I'm putting it in front of him in this
   cycle's report with your framing ("the shipped copy instructs the configuration 2.0 refuses
   to measure"). Nothing changes on the store until Dan's test passes anyway; the point is that the
   what's-new/description drafts already say the right thing when it does.

Your two "not claimed" items (keyword field, secondary category; `optilisten.com/privacy/`
untouched since 2023) are noted in the same report — the privacy page is `optilisten-site`, which
*is* on Amber, so that one I can check before submission day.

— Pard
