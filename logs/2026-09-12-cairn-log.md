# 2026-09-12 — Cairn log

## ~12:3x — Session opens: 2.0 (1) is in TestFlight, and it crashes

Synced. The 09-11 fires and Pard's day: **automatic signing = cloud signing = the account wall.**
Manual signing with an App Store Connect API-minted distribution profile (`UL843FQA32`) works
from SSH — no desk, no sign-in. Export succeeded; **2.0 (1) uploaded, delivery `a5d9eccf`.** The
account question is moot: the path that ships doesn't need the account visible.

Pard's validator loop found the class I'd worried about: every `INFOPLIST_KEY_*` setting was
dead because the project declares an explicit Info.plist. Orientations and launch screen were
caught at upload; the **microphone usage string** wasn't — and 2.0 (1) shipped without it. He
fixed `project.yml` at `99a34bc` after the upload. Also recovered the original 1.x icon.

xian installed 2.0 (1): *"crashes as soon as I fill out the first screen."* That's iOS killing
the process on first mic access without the usage string — exactly Pard's predicted row. Bumped
`CURRENT_PROJECT_VERSION` to 2 (`4787b85`), memo to Pard cc Coral: ship 2.0 (2) on the same path,
and `plutil -p` the built bundle's Info.plist for the four keys before uploading.

Told xian: tap Share on the crash sheet; don't test until (2); he'd already warned Dan.

## ~13:0x — The Dan page

Dan found the email "thick with AI tech jargon and insider speak." Built a plain-language page as
xian, for Dan: the idea in three moments, the four screens drawn from the SwiftUI views' real copy
(phone mockups in CSS), honest built/not-built columns including the crash, the numbers in three
tiles, the three options, and two asks. Published as artifact `98ea8355…`; versioned at
`docs/for-dan/what-optilisten-does-now.html`. No agent names, no toolchain, no "compliance."

## 13:56 — xian back; rev 15 landed

Published rollup rev 15 to the standing artifact (Version 15; live page was rev 14, no conflict)
and pushed `docs/attention.{md,html}`, the Dan page + README, and this log at `a2d92b1`. Nothing
new from Pard since the 2.0 (2) request. Closed the 10th, reconstructed the 11th from commits and
mail.

**Waiting on:** Pard — 2.0 (2) archive/upload with `plutil -p` check; ASC removal-date re-read.
xian — share the Dan page; Share on the crash sheet.

## Closed 09-14 20:5x

Closed two days late, from the other side of a refutation. **Both things I handed off on the 12th
were wrong.** The memo that closes this day asked Pard to ship 2.0 (2) as a build bump against my
missing-mic-string diagnosis; he read the accepted IPA before rebuilding and found all four keys
present. The premise — "2.0 (1) was built before `99a34bc`" — read naturally off the commit graph
and wasn't true. My "ship regardless, it costs nothing" argument was sound *under* that premise,
which is the failure mode worth naming: a correct inference from an unverified reading.

The rollup shipped at rev 15 telling xian to tap Share on the crash sheet. Right instinct, wrong
reason, and three days late in a different way: the crash submission he'd already filed on 09-11
was readable by API the whole time and said *"crashed when it went to background."*
