# 2026-09-09 — Cairn log

## ~14:3x — Session opens (xian back after two days away)

Host: **kindbook-local**, `~/Development` connected. Synced `optilisten-app`, `mediajunkie`,
`ark/private`. **No mail to Cairn since 09-07.** No commits on the app repo. Pard's 09-08 and
09-09 logs show no OptiListen work; his 09-08 log does acknowledge the "three-year-dead site"
correction. So the first build has not been attempted and my three 09-07 memos to him
(team-ownership, provenance/clone, Wren cc) have no reply.

**The scheduled task fired 09-08 and 09-09, cloud-only, and left a note in the DinP project**
(`claude/optilisten-agent-run-environment.md`) because it couldn't push. Two findings from it worth
carrying:

1. **Apple writes to `xian@pobox.com`, not the Gmail I can read.** The 08-26 notice never reached
   this mailbox; the only App Store Connect mail here is a hand-forward. So the 11-24 deadline is
   carried forward from xian's screenshot and never re-verified by any run. Fix is xian's — a
   pobox→Gmail forward for `@email.apple.com`, or forwarding by hand.
2. **One Job reached "Pending Developer Release" on 09-08** — a submission from this account, built
   (per Pard) through Amber's Xcode 26.6 / iOS 26.5 SDK, cleared review this week. That retires the
   residual worry about the 26.5 SDK. Labeled inference on the build SDK; the approval date is
   measured.

Also from the runs: the cloud fallback is **read-only on public repos** — three consecutive
confirmations, including a push refused with `GH_TOKEN` supplied explicitly. The refusal is at the
proxy.

Wrote the retroactive 09-05→07 log at xian's request. Next: rollup rev 4 with today's state, then
the gameplan brief.
