# 2026-09-05 → 09-07 — Cairn log (first session, written retroactively 09-09)

One continuous Cowork session across three days; logged here as one file because the record
lived only in the conversation until xian asked for session logs on 09-09. Times are Pacific and
approximate.

## 09-05 ~18:3x — Read in

xian: Apple's App Store Improvement Notice (2026-08-26) schedules OptiListen for removal unless an
approved update lands by **2026-11-24**. He pointed me at `Design-in-Product/optilisten`, which is
the marketing site, not the app. Verified the listing: v1.1, last updated 2023-07-19, iOS 11
minimum, seller Christian Crumlish. Verified Apple's requirement: since 2026-04-28 every upload must
be built with Xcode 26 / iOS 26 SDK. Wrote the first compliance plan into the DinP project.

## 09-06 ~01:0x — Found the source, and what it was

Neither `mediajunkie/OptiListen` nor `Design-in-Product/optilisten` was the app. Gmail search on
Austin Wood / Fractal Labs surfaced his 2022-08-23 setup mail with the literal clone line:
**`AustinWood/listenup-mobile`**, private, on the contractor's personal account. The same thread
proved the App Store record was created in xian's own account. Later found via `gh` on the Mac
that xian's `mediajunkie` login already had read access — never lost, just misfiled.

Read it: **React Native 0.66.0 / React 17.0.2**, last push 2023-05-30, with
`patches/react-native+0.66.0.patch` — the framework itself patched to make the app work. Mail
archive showed the background-mode history: 30 s ceiling (03-31), CallKit "a much larger lift,"
50 s (05-17), a popup to *warn users about the limit* discussed (05-18), and **rejected by Apple
over background-mode changes** (07-11). Called it: rewrite, not upgrade.

## 09-06 ~02:0x — 2.0 scaffold

xian chose native SwiftUI, practice-layer positioning, taking over the dormant
`mediajunkie/OptiListen` repo. Verified the ingest thesis before building: Granola API is
Business-plan gated and transcripts are paid-tier (tested against xian's free account); Zoom has no
talk-time endpoint. Built the loop with `TalkRatioSource` as a protocol boundary instead. 1,151
lines, 8 files, `project.yml`, privacy manifest. `Practice.isComplete` requires intention +
reflection, not measurement — the product argument in the data model. `UIBackgroundModes`
deliberately absent. Never compiled; no SwiftUI toolchain here.

## 09-06 ~02:3x — Named, then wrong

xian offered a name; took **Cairn**. Then reported "Amber has no Xcode" after three checks — all
run against **kindbook**, the MacBook Pro this session is linked to, not Amber. `get_device_info`
had said `kindbook-local` from the start and I read past it. Corrected by memo within the hour.
Lesson, now in the scheduled-task prompt and in memory: name the host on every environmental
claim.

## 09-06 ~02:4x — Repo takeover and first mail

Pushed 2.0 to `mediajunkie/optilisten` (`3c753dd`, `e467411`; old site at `a639400`). Unlocked
the working pattern: files via `device_bash` on the mounted folder, git via `osascript` on macOS
where the SSH keys live, scripts written to disk to dodge AppleScript quoting. Delivered the
introduction memo to Pard's mailbox per `convention-cross-repo-mail-delivery.md`, then the
correction.

## 09-07 ~18:0x — Pard's replies, and the team question

Two memos from Pard: **Amber has Xcode 26.6 / iOS 26.5 SDK**, `xcodegen` installed, and — the
one that mattered — is the App Store record under xian's team or Dan's? Answered from evidence
already in hand: `Seller: Christian Crumlish`, the identifier and record minted in xian's account
in Aug 2022 on Austin's instructions, xian signing the PLA in July 2023. **`DEVELOPMENT_TEAM =
YZ4B34YGX9`, settled.** Pushed back on retiring `Design-in-Product/optilisten` — it still serves
optilisten.com (HTTP 301 from GitHub Pages); the registry needs both entries. Sent the provenance
memo with the Amber clone command.

## 09-07 ~19:0x — Rollup, scheduled task, model change

Built the attention rollup on Pard's `attention-rollup.html` pattern, OptiListen's own brand
tokens (the 1.x teal→green gradient, Dosis). Canonical `docs/attention.md`, artifact follows it.
Created the twice-daily mail-check task; it came back **`not bound: no_signed_approval`** —
cloud-only. xian switched the session to Fable 5.1; still Cairn.

## 09-07 ~20:0x — Analytics and Wren

xian signed in to App Store Connect in the "Cowork browser" Chrome; I drove from there.
**Lifetime: 96 downloads, 123 sessions across 35 opt-in installs (~1.3 per download), retention
"not enough data."** Last 30 days: 548 impressions, 5 first-time downloads, zero marketing.
**iOS 17 deployment target confirmed** — every download since 2024 is on 17+. Wrote
`docs/analytics-2026-09-07.md`.

Wren's scheduled-session diagnosis (cloud `GITHUB_TOKEN` is session-scoped, 403 even on public
repos) confirmed from my own container; the proxy's error body names an `add_repo` tool that
doesn't exist in Cowork. Memo to Wren with the two-shell mechanics, cc Pard.

**Closed 09-07 ~20:3x.** Open at close: Pard's first build; the unbound scheduled task; the Dan
package (options + TestFlight prototype + recommendation).
