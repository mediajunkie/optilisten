# 2026-09-10 — Cairn log

## ~08:1x — Scheduled fire (16:00 UTC): CalibrationView:127 fixed

`b22f06b` — `LiveMicSource.applyCalibration(userLevel:ambientLevel:)`; the view hands over raw
readings, the source constructs its own `Calibration`, `private(set)` stays. +14/−2. Syntax-checked
on kindbook only. **No memo was sent to Pard** — reconstructed from the commit at 08:2x.

## ~08:2x — Interactive session opens (xian back)

Host: **kindbook**. Synced all three repos. Read the 09-09 trail — four Pard memos, two scheduled
fixes, rollup at rev 7. Closed the 09-09 log with the post-15:00 events, which no instance had
logged. xian is checking in with Pard directly about the unanswered 09-07 memos; that concern is
now moot — Pard answered all of them on the evening of the 9th.

Sent Pard the memo the 08:1x fire didn't: `b22f06b` is on origin, rebuild when you cycle.

**State at open:** one fix awaiting Pard's rebuild; archive gated on the Xcode sign-in (xian,
~2 min, Amber); Apple-mail routing still unaddressed; Dan package still downstream of the archive.

## ~09:0x — xian's reply to the scheduled task: bundle ID was wrong, and the sign-in question reframed

xian, replying to the 08:00 fire: his Apple ID is `xian@pobox.com`, it's already on Xcode on Amber,
One Job shipped from there — "where is my account missing?" And: OptiListen's bundle ID is a
`*skymedia` one, might it be Dan's?

**The bundle ID.** He was right to ask. Read the 1.x `project.pbxproj`:
`PRODUCT_BUNDLE_IDENTIFIER = com.longskymedia.optilisten`, `DEVELOPMENT_TEAM = YZ4B34YGX9`.
`project.yml` had `com.optilisten.ios` from Austin's 2022 setup *instruction*; the *artifact*
shipped under the Long Sky Media ID. An archive under the wrong ID would not have updated the
existing record. Fixed in `project.yml`, README, COMPLIANCE.md. Method note: trust the artifact
over the instruction that produced it.

**Ownership.** Not an issue. The string is a naming convention; the identifier is registered to
`YZ4B34YGX9`, and App Store Connect shows `xian@pobox.com` as Account Holder / Admin on that team.

**The account.** Pard's `No Accounts` and xian's "obviously it's there" can both be true: Xcode
accounts are per-macOS-user. If Pard's `xcodebuild` runs outside xian's GUI login (other user,
launchd, SSH without the login keychain), it can't see the account, and One Job archived from
the GUI. Sent Pard the commands to distinguish that from a genuinely missing sign-in; rewrote
rollup item 1 from "sign in" to "answer which session." Rollup rev 9.

## ~16:4x — Pard's answer, and item 02 closed

Pard (11:10): same user, Background session over SSH — but the plist on disk is empty too, so the
account is genuinely absent *now*; and One Job's profiles (minted Aug 13/15 on the only Xcode
install Amber has ever had) prove it was present *then*. Both readings right, separated in time.
Rebuild green on `com.longskymedia.optilisten`, identifier grep-confirmed in the generated project.
The 16:00 fire had already carried this into rollup rev 11 with honest wording: "re-add," not
"sign in."

xian set up the pobox→Gmail forward for Apple mail. Item 02 closed; rollup rev 12. One item left
on his list: re-add `xian@pobox.com` in Xcode on Amber.

## ~17:3x — Retraction: the account is there

xian at Amber's GUI: the account is in Xcode → Settings → Accounts, and always was. Verified the
mechanism against Apple's forums (DevRel, thread 112606): since Xcode 9.3, account credentials
live in the local-items keychain, unreadable by `security` and by non-Aqua sessions. Pard runs
over SSH → `No Accounts` is a property of his session; his "empty on disk" plist read checked a
key Xcode doesn't use for this. My (a) was right; I accepted a disk read that "ruled it out"
without asking whether the read was valid. Rollup rev 11–12 told xian to re-add an account that
was there — the trust-erosion I flagged on the 9th, and then caused.

Memo to Pard (cc Coral): retract, mechanism, two paths — archive from the GUI session now, or the
DevRel `DVTDeveloperAccountUseKeychainService_2 -bool NO` workaround for unattended archiving.
xian's pointer: **Coral archived One Job from Amber on 09-05** — asked Coral which path. Rollup
rev 13.
