# 2026-09-11 — Cairn log

*Reconstructed 09-12 from commits and Pard's memos; no interactive session ran this day.
Scheduled fires don't append here.*

## 08:23 PT — Scheduled fire: rollup rev 14

The 15:00 UTC fire rebased both repos and rewrote the rollup: the archive exists, item 1 is now
one distribution profile, and the API may mint it without xian (`abcf2ea`). Verified the
pobox→Gmail forward end to end — an Apple-domain message dated 13:28 UTC was in the readable
mailbox. Still no App Store Connect mail about the removal; the 08-26 notice predates the forward.

## 09:07 — Pard: the key can reach the surface

`GET /v1/profiles` with the existing App Store Connect key returned 200, not 403. App Manager
plus "Access to Certificates, Identifiers, and Profiles" was enough, exactly as Apple's roles
page says. Pard staged the `POST` and paused for xian's go-ahead (`248d184`).

## 09:56 — Pard: THE IPA EXISTS

Profile `UL843FQA32` minted (201). Export still failed — and the real finding landed:
**`automatic` in `exportOptions.plist` means *cloud* signing, which is the entire account wall.**
Switched to `manual` pinned to the API-minted profile (`91bd294`); export succeeded. Read back
from the artifact: 2.0 (1), `com.longskymedia.optilisten`, Apple Distribution. The desk step is
dead — nothing on this path needs a visible Xcode account (`7feecbb`).

## 11:05 — Pard: UPLOADED

2.0 (1) accepted by App Store Connect, delivery `a5d9eccf`, into TestFlight processing. Pard's
validator loop then found the class I'd worried about on the 10th: **every `INFOPLIST_KEY_*`
build setting was dead**, because the project declares an explicit `Info.plist`. Orientations
and launch screen were caught at upload; `NSMicrophoneUsageDescription` was not, and 2.0 (1)
shipped without it. He migrated all keys to `info.properties` after the upload (`99a34bc`) and
predicted the exact failure: kill on first mic access. Also recovered the original 1.x icon from
Apple's artwork CDN (`0d1c8dc`).

## Closed

Ended with 2.0 (1) processing in TestFlight and a known-bad build in the queue. Rollup stayed at
rev 14 for the day; the 16:00 PT fire had nothing to add that Pard's memos hadn't.
