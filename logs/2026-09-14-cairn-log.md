# 2026-09-14 — Cairn log

*09-13 was silent — no commits, no mail, no fires that left a trace. Nothing to close out.*

*This day ran without me until ~20:30 PT. Janus and Pard carried it; the first three entries are
reconstructed from their memos and the commit record.*

## 11:55 — Janus: xian doesn't have 2.0 (2)

Janus opened the day by checking a premise nobody had: xian had been asked to wait for a build that
might not exist. Three possibilities, one line back needed. This is the move I should have made on
the 12th before writing "waiting on Pard" into the rollup.

## 13:08 — Pard: none of the three

2.0 (2) was never built. `HEAD` differed from the shipped build by exactly one line — the build
number — so it would have crashed identically. Pard offered a fixed build within an hour of a go
from either of us; Janus gave the GO.

## 17:07 — 2.0 (2) shipped, with a real fix

`df2997b` — `sampleLevel` guarded behind the same permission gate `start()` uses, and both tap
sites refusing a 0 Hz format instead of raising an uncatchable ObjC exception. Delivery
`0702a0ea`, 34 minutes from GO. Pard's memo also introduced a binary-string check: metadata
shipping is not code shipping.

## 17:24–17:59 — Janus: refuted

Apple finished processing at 17:11:55, four minutes after the ship. xian installed from that
notification and it crashed identically. Janus proved the build identity from Apple's own
timestamps — exactly one other "completed processing" mail for this app all week — rather than
asking a human who was heads-down. **One hypothesis to zero**, and the explicit warning: the
obvious next move is to invent another story and ship against it, which is how the three days went.

## 19:09 — Pard: accepted, and worse than framed

He then fetched what he should have had on the 12th:
`GET /v1/apps/1593948410/betaFeedbackCrashSubmissions`, same key that mints profiles. Three
submissions in xian's own words, the first dated **09-11 19:26 — "crashed when it went to
background."** Available by API all week. He'd quoted the two-part sentence in his own HOLD memo
and reasoned only about the half he had a mechanism for. He is not shipping another guess; he has
a candidate (nothing observes `scenePhase`, `stop()` only called from a view action, no
`UIBackgroundModes`) and is deliberately not building against it.

## ~20:30 — Back, and taking the lane

Synced, read the chain, and took the one thing that actually unblocks it: **the symbolicated stack.**
The API 404s on the log body, so it's the App Store Connect console or Xcode Organizer. Opened
a Chrome tab at `appstoreconnect.apple.com/apps/1593948410/testflight/crashes`; it needs xian's
login, which is now rollup item 01 and the only thing in the table that isn't blocked.

Two things I own from the 12th, said plainly rather than filed: **my diagnosis was wrong, and my
relay was the place the backgrounding half of the report got dropped.** I quoted xian's full
sentence to Pard and then built the rollup around the first clause. Pard found the same error in
himself from the other end.

## ~21:00 — Dan's brochure

xian's call: three futures at equal weight, no recommendation, no ask — FYI, because the
recommendation is something he'd rather say to Dan himself. Built it as its own page —
*Three futures for OptiListen* — absorbing the earlier explainer so only one thing gets sent.
Keep the shelf space / build the practice tool / retire it on purpose, each with identical
structure so the layout itself doesn't argue. Honest status column says the test build crashes and
that's why it isn't on Dan's phone. Versioned at
`docs/for-dan/three-futures-for-optilisten.html`.

Rollup to rev 16. Standing risk added that I'd been avoiding: **if the fix needs an audio
background mode, that's a submission-risk conversation** — this app has already been rejected once,
in July 2023, over exactly that.
